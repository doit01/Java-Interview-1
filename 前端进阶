https://chat.deepseek.com/share/j8nyst8bdgu21arts8
Promise/Async async/await 是什么，解决了什么，注意事项是什么？
async/await 是 Promise 的语法糖，让异步代码写起来像同步代码。错误处理推荐使用 try-catch，但要注意 try-catch 只能捕获 await 所在的错误，未 await 的 Promise 或回调中的错误仍需 .catch。
同时要避免在循环中滥用 await 导致性能问题，可以用 Promise.all 并发执行。

开始Vue 3项目
    ↓
选择构建工具(Vite)
    ↓
项目初始化 + 安装核心依赖
    ├── Vue 3
    ├── Vue Router (SPA需要)
    ├── Pinia (状态管理)
    └── Tailwind CSS
    ↓
配置Tailwind (tailwind.config.js)
    ↓
组件开发流程：
    1. 创建.vue单文件组件
    2. 使用<script setup>语法糖  https://chat.deepseek.com/share/1vlgnq1ef0srk5ek48
    3. 定义响应式数据(ref/reactive)
    4. 计算属性(computed)和侦听器(watch)
    5. 生命周期钩子(onMounted等)
    6. 模板编写(结合Tailwind类)
    7. 样式作用域(scoped或CSS模块)
    ↓
路由配置(如需)
    ↓
状态管理设计(如需)
    ↓
优化与打包

<script setup>语法糖{
与普通 <script> 的区别：无需 export default，顶层变量自动暴露。
无法定义 name 选项（可通过额外 <script> 或 vite-plugin-vue 解决）。
不支持 inheritAttrs、components 等选项，需使用 defineOptions 或普通 <script> 配合。

编译器宏的使用：
defineProps：声明 props，支持运行时声明和纯类型声明。
defineEmits：声明可触发的事件。
defineExpose：暴露组件内部属性/方法给父组件的模板 ref。
defineOptions（Vue 3.3+）：定义组件名、inheritAttrs 等。
这些宏无需手动导入，编译时自动处理。

响应式 API 的使用：ref、reactive、computed、watch 等用法与普通 setup() 完全一致。
注意 ref 在模板中自动解包，但在函数中需 .value。
组件递归 / 动态组件： 递归组件需要显式指定 name（使用 defineOptions 或第二个 <script>）。
动态组件 <component :is="Component"> 直接传入组件对象即可。

生命周期钩子：可直接调用 onMounted、onUpdated 等，无需写在 setup() 内部。
与 TypeScript 的结合
defineProps<{ msg: string }>() 实现纯类型声明。
从 vue 导入 PropType 用于复杂类型。

编译时优化原理
模板中的变量引用被静态提升，减少运行时代理。
编译后的代码比普通 setup() 更简洁高效。
}


浏览器原生功能：
1 输入框聚焦了，父元素也该有反应。
用focus-within
2 对于没网络了情况，页面应该怎么办？
浏览器最基础能力windows.addEventListener("offline")
windows.addEventListener("online")
3 不重要的任务慢慢跑：
function showMessage(){
console.log(" ran when browser was idle");
}
if("requstIdleCallback" in window){
requstIdleCallback(showMessage)
}
适合：分析埋点，可以延迟的非关键任务，后台数据整理，琐碎工作。

4 如何让组件根据自身容器尺寸来响应，而不是盯着整个屏幕视口？
用container query。如：@用container （min-width:400px）
5 唯一id 用const bytes=new Uint8Array(8);
crypto.getRandomValues(bytes);
const uniqueID=Array.from(bytes).map(b=>b.toString(16).padStart(2,"0")).join("");
6 别一做弹窗就装modal库。
简单用<dialog>.
它自带了对话框行为，焦点管理更自然，可访问性更好，背景区域会失活，ESC可关闭，不必靠一堆css，js拼出来。


组合式API对比选项API ?
https://chat.deepseek.com/share/ax5600ycq181ccngtq
为什么组合式 API 更优？
对比维度	选项式 API	组合式 API
逻辑聚合	一个功能的代码（data/methods/mounted）散落在各处	 ;  所有相关代码（状态、方法、生命周期）写在一起，内聚性强
逻辑复用	需要 mixins，存在命名冲突、隐式依赖等问题	 ; 封装成普通函数（useFetch），在任何组件中直接调用，清晰且无副作用
TypeScript 支持	this 上下文类型推导困难 ; 	普通变量和函数，类型推断完美
代码组织	文件变大后难以维护（上千行 options 混乱）	;按功能拆分多个 useXXX 函数，每个函数职责单一
Tree Shaking	未使用的生命周期/选项无法被摇掉; 	只导入用到的 API（ref、onMounted），打包更小

CSS3 关键特性
css
复制
/* Flex布局 */
.container {
  display: flex;
  justify-content: center;  /* 主轴对齐 */
  align-items: center;      /* 交叉轴对齐 */
  flex-wrap: wrap;         /* 换行 */
}

/* Grid布局 */
.grid-container {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 1rem;
}

/* CSS变量 */
:root {
  --primary-color: #3b82f6;
  --spacing: 1rem;
}
.element {
  color: var(--primary-color);
  margin: var(--spacing);
}
